import 'package:flutter/material.dart';

class ListModel {
  String title;
  String subTitle;
  IconData icon;

  ListModel({
    required this.title,
    required this.subTitle,
    required this.icon,
  });
}
